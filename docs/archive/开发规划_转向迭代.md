# SiliconMomo 转向迭代开发规划

> 基于 PRD_转向.md 的详细开发实施计划

## 📊 当前状态分析

### 已有优势
- ✅ **L4级自动化架构**：CDP接管 + RecoveryAgent 自动修复（技术壁垒）
- ✅ **完整Agent体系**：WriterAgent、ArtistAgent、RecoveryAgent、Supervisor
- ✅ **即梦自动化**：已实现图片生成流程
- ✅ **知识库系统**：素材收集与存储机制

### 需要改造的核心问题
- ❌ **WriterAgent**：当前依赖 `knowledge_base.py` 的RAG逻辑，需要改为情绪模板库
- ❌ **ArtistAgent**：当前根据文案生成图片Prompt，需要改为固定Prompt库
- ❌ **互动逻辑**：当前分析技术内容，需要改为"蹭流量"模式
- ❌ **Supervisor**：当前是复杂循环，需要简化为线性流水线
- ❌ **缺少批量生产模式**：没有囤货+分发机制

---

## 🎯 迭代目标

### 阶段一：架构简化（Day 1-3）
**目标**：完成核心模块改造，实现"发疯文学"模式

### 阶段二：批量生产（Day 4-7）
**目标**：实现囤货模式，积累素材库

### 阶段三：自动化发布（Day 7+）
**目标**：实现定时发布，数据反馈优化

---

## 📋 详细开发任务清单

### 任务1：创建情绪模板库系统 ⭐⭐⭐

**文件**：`data/emotions.json`（新建）

**内容结构**：
```json
{
  "styles": {
    "发疯": {
      "keywords": ["周一", "下班", "加班", "周一哪有不疯的", "确诊为淡淡综合症"],
      "prompt_template": "你是一个崩溃的打工人，用感叹号和咆哮体，抱怨生活...",
      "emoji_pool": ["😭", "💀", "🔥", "🤡", "💔"],
      "tone": "激烈/咆哮"
    },
    "治愈": {
      "keywords": ["晚安", "温暖", "治愈", "温柔"],
      "prompt_template": "你是一个温柔的邻家姐姐，晚安语录，温暖人心...",
      "emoji_pool": ["🌙", "✨", "💕", "🌸", "☀️"],
      "tone": "温柔/温暖"
    },
    "清醒": {
      "keywords": ["恋爱脑", "醒悟", "毒舌", "骂醒"],
      "prompt_template": "毒舌闺蜜，骂醒恋爱脑...",
      "emoji_pool": ["💅", "🙄", "👀", "💯"],
      "tone": "毒舌/清醒"
    },
    "共情": {
      "keywords": ["深夜emo", "独居", "孤独", "生活感悟"],
      "prompt_template": "深夜emo文案，共情打工人...",
      "emoji_pool": ["🌃", "💭", "😔", "🌧️"],
      "tone": "共情/emo"
    }
  },
  "image_prompts": {
    "发疯": [
      "cinematic lighting, cyberpunk cityscape, rain, neon lights, melancholic mood, blur effect",
      "dark atmosphere, abstract geometric shapes, high contrast, dramatic shadows"
    ],
    "治愈": [
      "cinematic lighting, asian girl, blurred background, side profile, warm sunset, melancholic mood",
      "soft lighting, gentle colors, peaceful landscape, morning mist"
    ],
    "清醒": [
      "minimalist composition, modern aesthetic, clean lines, professional photography",
      "urban street scene, natural lighting, candid moment"
    ],
    "共情": [
      "night cityscape, window view, warm indoor lighting, cozy atmosphere",
      "lonely figure silhouette, vast landscape, contemplative mood"
    ]
  }
}
```

**修改文件**：
- `core/writer.py`：移除 `pick_inspiration()` 和 `write_from_inspiration()` 的RAG逻辑
- 新增 `write_from_emotion_template()` 方法

---

### 任务2：改造 WriterAgent ⭐⭐⭐

**文件**：`core/writer.py`

**改造点**：

1. **移除RAG依赖**
   - 删除 `_load_inspirations()` 中对 `knowledge_base.py` 的依赖
   - 删除 `pick_inspiration()` 方法（或改为从情绪库选）

2. **新增情绪模板生成**
   ```python
   def write_from_emotion_template(self, style="发疯"):
       """基于情绪模板生成文案"""
       # 1. 加载 emotions.json
       # 2. 随机选择关键词
       # 3. 调用LLM生成文案（使用模板Prompt）
       # 4. 返回 {title, content, image_prompt, style, tags}
   ```

3. **Prompt调整**
   - 从"总结文章核心观点"改为"模仿小红书爆款文风"
   - 强调：多用Emoji、短句、语气激烈/阴阳怪气/温柔刀

**预期输出格式**：
```json
{
  "title": "周一哪有不疯的 😭💀",
  "content": "又是周一...\n\n为什么周末只有两天？\n为什么周一要上班？\n为什么我的精神状态这么稳定？\n\n（稳定地发疯）🔥",
  "image_prompt": "cinematic lighting, cyberpunk cityscape, rain, neon lights",
  "style": "发疯",
  "tags": ["#发疯文学", "#打工人", "#周一"]
}
```

---

### 任务3：改造 ArtistAgent ⭐⭐

**文件**：`core/artist.py`

**改造点**：

1. **移除动态Prompt生成**
   - 不再根据文案内容生成图片Prompt
   - 改为从固定Prompt库随机选择

2. **新增固定Prompt库**
   ```python
   def get_image_prompt_by_style(self, style="发疯"):
       """根据风格从固定库选择Prompt"""
       # 从 emotions.json 的 image_prompts[style] 随机选一个
   ```

3. **批量生成模式**
   - 新增 `batch_generate_images(count=50, style=None)` 方法
   - 支持凌晨2-5点批量生产图片入库

**修改调用链**：
- `run_creation.py`：改为使用固定Prompt库
- 新增 `run_batch_generation.py`：批量生产脚本

---

### 任务4：简化 Supervisor ⭐⭐

**文件**：`core/supervisor.py`

**改造点**：

1. **从循环改为线性流水线**
   - 移除复杂的"感知-思考-决策-执行"循环
   - 简化为：**囤货模式** → **分发模式**

2. **新增模式切换**
   ```python
   async def start_shift(self):
       current_hour = datetime.now().hour
       
       if 2 <= current_hour <= 5:  # 凌晨低峰期
           await self.batch_mode()  # 囤货模式
       else:  # 高峰时段
           await self.dispatch_mode()  # 分发模式
   ```

3. **囤货模式（Batch Mode）**
   - 调用 `ArtistAgent.batch_generate_images(50)`
   - 调用 `WriterAgent` 生成50条候选文案
   - 存入 `data/drafts.json`

4. **分发模式（Dispatch Mode）**
   - 从 `data/drafts.json` 随机抽取一条
   - 调用 `Publisher` 发送
   - 不再每次发帖都走全流程

---

### 任务5：改造互动逻辑 ⭐

**文件**：`actions/interaction.py`

**改造点**：

1. **评论逻辑改为"蹭流量"**
   - 原逻辑：分析帖子内容，发表见解
   - 新逻辑：在热门情感贴下评论简单互动
   - 评论模板：`["太真实了", "演我精神状态", "吸好运", "狠狠共情了"]`

2. **修改LLM分析Prompt**
   - 文件：`core/llm_client.py`
   - 从"技术博主"改为"情感博主"
   - 判断标准从技术内容改为情感内容

**修改方法**：
```python
# core/llm_client.py
def analyze_and_comment(self, title, content):
    prompt = f"""
    你是一个活跃在小红书的情感博主"Momo"。
    
    【判断标准】
    1. 帖子必须属于情感类：发疯文学、共情文案、治愈文案、emo文案
    2. 如果是热门帖子（点赞>100），优先评论蹭流量
    
    【评论风格】
    - 简单直接："太真实了"、"演我精神状态"、"吸好运"
    - 不要长篇大论，10字以内
    """
```

---

### 任务6：创建批量生产脚本 ⭐⭐

**文件**：`run_batch_generation.py`（新建）

**功能**：
- 凌晨2-5点自动运行
- 批量生成图片（50张）
- 批量生成文案（50条）
- 存入素材库

**代码结构**：
```python
async def main():
    # 1. 连接浏览器
    bm = BrowserManager()
    await bm.start()
    
    # 2. 初始化Agent
    writer = WriterAgent(recorder)
    artist = ArtistAgent(bm.page, recorder)
    
    # 3. 批量生图（固定Prompt库）
    styles = ["发疯", "治愈", "清醒", "共情"]
    for style in styles:
        for i in range(12):  # 每个风格12张
            prompt = artist.get_image_prompt_by_style(style)
            image_path = await artist.generate_image(prompt)
            # 保存到素材库
    
    # 4. 批量生成文案
    for i in range(50):
        draft = writer.write_from_emotion_template(
            style=random.choice(styles)
        )
        # 随机匹配一张图
        # 保存到 drafts.json
```

---

### 任务7：优化发布流程 ⭐

**文件**：`actions/publisher.py`

**改造点**：

1. **从草稿库读取**
   - 不再每次现写现画
   - 从 `data/drafts.json` 读取 `status="ready_to_publish"` 的草稿

2. **定时发布**
   - 在 `config/settings.py` 配置发布时间点：`[8, 12, 21]`
   - Supervisor 在指定时间触发发布

3. **发布后标记**
   - 调用 `writer.mark_draft_published()`

---

### 任务8：更新配置文件 ⭐

**文件**：`config/settings.py`

**新增配置**：
```python
# === 🎭 情绪模板配置 ===
EMOTIONS_FILE = DATA_DIR / "emotions.json"

# === 📅 批量生产配置 ===
BATCH_GENERATION_HOURS = [2, 3, 4, 5]  # 凌晨低峰期
BATCH_IMAGE_COUNT = 50  # 每次生成图片数
BATCH_DRAFT_COUNT = 50  # 每次生成文案数

# === 🚀 发布配置 ===
PUBLISH_HOURS = [8, 12, 21]  # 早中晚高峰
DAILY_PUBLISH_LIMIT = 3
```

---

## 🔄 开发优先级

### P0（必须完成）
1. ✅ 创建 `data/emotions.json` 情绪模板库
2. ✅ 改造 `core/writer.py`：移除RAG，新增情绪模板生成
3. ✅ 改造 `core/artist.py`：使用固定Prompt库
4. ✅ 创建 `run_batch_generation.py` 批量生产脚本

### P1（重要）
5. ✅ 简化 `core/supervisor.py`：实现囤货+分发模式
6. ✅ 改造 `actions/interaction.py`：评论改为"蹭流量"
7. ✅ 更新 `config/settings.py`：新增配置项

### P2（优化）
8. ⚪ 优化 `actions/publisher.py`：从草稿库读取
9. ⚪ 添加数据统计：记录浏览量、点赞数
10. ⚪ 实现A/B测试：不同风格文案的流量对比

---

## 📝 实施步骤

### Step 1: 准备阶段（1小时）
- [ ] 创建 `data/emotions.json` 文件
- [ ] 准备固定图片Prompt库（每个风格10-15个Prompt）
- [ ] 备份当前代码

### Step 2: 核心改造（4-6小时）
- [ ] 改造 `WriterAgent`：实现 `write_from_emotion_template()`
- [ ] 改造 `ArtistAgent`：实现 `get_image_prompt_by_style()`
- [ ] 测试单次生成流程

### Step 3: 批量生产（2-3小时）
- [ ] 创建 `run_batch_generation.py`
- [ ] 测试批量生成图片（小批量：5张）
- [ ] 测试批量生成文案（小批量：5条）

### Step 4: 流水线改造（2-3小时）
- [ ] 简化 `Supervisor`：实现模式切换
- [ ] 实现囤货模式
- [ ] 实现分发模式

### Step 5: 互动优化（1-2小时）
- [ ] 修改 `LLMClient.analyze_and_comment()` Prompt
- [ ] 更新评论模板为"蹭流量"风格

### Step 6: 测试验证（2-3小时）
- [ ] 端到端测试：批量生成 → 发布 → 互动
- [ ] 验证RecoveryAgent在发帖流程中的自动修复能力
- [ ] 观察小红书后台数据（浏览量）

---

## 🎯 成功指标

### 技术指标
- ✅ 批量生成50张图片成功率 > 90%
- ✅ 批量生成50条文案成功率 > 95%
- ✅ 自动发布成功率 > 85%（RecoveryAgent兜底）

### 业务指标（Day 7+）
- 📊 单篇笔记浏览量 > 100（初期目标）
- 📊 每日发布3篇，持续7天无中断
- 📊 不同风格文案的A/B测试数据

---

## ⚠️ 风险与应对

### 风险1：即梦生图不稳定
**应对**：增加重试机制，RecoveryAgent自动修复

### 风险2：情绪模板库Prompt效果不佳
**应对**：人工筛选一次，建立优质Prompt库

### 风险3：小红书风控
**应对**：保持HumanMotion拟人化，控制发布频率

---

## 📚 相关文件清单

### 需要修改的文件
- `core/writer.py` ⭐⭐⭐
- `core/artist.py` ⭐⭐
- `core/supervisor.py` ⭐⭐
- `core/llm_client.py` ⭐
- `actions/interaction.py` ⭐
- `config/settings.py` ⭐

### 需要新建的文件
- `data/emotions.json` ⭐⭐⭐
- `run_batch_generation.py` ⭐⭐

### 可选优化文件
- `actions/publisher.py`（优化从草稿库读取）
- `main.py`（添加定时任务）

---

## 🚀 下一步行动

1. **立即开始**：创建 `data/emotions.json` 并填充初始数据
2. **并行开发**：改造 `WriterAgent` 和 `ArtistAgent`
3. **测试验证**：小批量测试（5张图+5条文案）
4. **逐步扩展**：扩展到50张+50条，验证稳定性

---

**预计总工时**：12-18小时  
**预计完成时间**：3-5天（按每天3-4小时计算）
